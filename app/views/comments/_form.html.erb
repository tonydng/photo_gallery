<h3>New Comment</h3>
<%= form_for [@photo, @comment] do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<%= f.label :content %><br/>
	<%= f.text_area :content, rows: 6 %>

	<p><%= f.submit %></p>
<% end %>