<%= form_for [@commentable, @comment] do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<p><b><%= f.label :content, "New Comment" %></b></p>
	<%= f.text_area :content, rows: 2 %>

	<p><%= f.submit class: 'btn btn-primary' %></p>
<% end %>